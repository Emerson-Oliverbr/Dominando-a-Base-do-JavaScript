const s = "abc"; // Tipo primitivo em Javascript "Não tem propriedades, não tem metodos e não tem funções"
/*
const v = new String(s);
const s2 = v.toUpperCase();

[O javascript faz isso implicitamente] Classes wrappers são objetos sua declaração a primeira letra é maiuscula 

/**
 * string -> String
 * number -> Number
 * boolean -> Boolen
 * */

console.log(s.toUpperCase());

